#include "main.h"
/**
 * _strcat - concatenates two strings
 * @dest: input value
 * @src: input value
 *
 * Return: void
 */
char *_strcat(char *dest, char *src)
{
	int ch, ch2;

	ch = 0;

	while (dest[c])
		ch++;

	for (ch2 = 0; src[ch2]; ch2++)

		dest[ch++] = src[ch2];

	return (dest);
}
